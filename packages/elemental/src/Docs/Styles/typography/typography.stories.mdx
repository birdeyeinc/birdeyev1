import React from 'react';
import typography from "./typography"
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="Styles/Typography" />

# Typography

<br/><br/>

## Size Variants

<br/><br/>
<div style={{width:"500px"}}>
    {Object.entries(typography.fontSize).map(([key,value]) => (
        <div style={{ marginBottom: '10px', display:"flex", justifyContent:"space-between", alignItems:"center" }}>
            <p style={{fontSize: value,}}>{value}</p>
            <div style={{padding:"8px 16px", backgroundColor:"lightgrey", borderRadius:"8px",  fontSize:"14px"}}>${key}</div>
        </div>
    ))}
</div>

<br/><br/>

## Text Weights

<br/><br/>

<div style={{width:"500px"}}>
    {Object.entries(typography.fontWeight).map(([key,value]) => (
        <div style={{ marginBottom: '10px', display:"flex", justifyContent:"space-between", alignItems:"center" }}>
            <p style={{fontWeight: value, fontSize: '24px'}}>{value}</p>
            <div style={{padding:"8px 16px", backgroundColor:"lightgrey", borderRadius:"8px "}}>${key}</div>
        </div>
    ))}
</div>

