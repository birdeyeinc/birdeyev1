import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import layoutAndSpacing from "./layoutAndSpacing";


<Meta title="Styles/Layout & Spacing" />

# Padding

<div style={{display:"flex", flexWrap:"wrap", gap:"40px"}}>
    {
        Object.entries(layoutAndSpacing.padding).map(([key,value])=>(

                <div style={{display:"flex", flexDirection:"column", alignSelf:"end"}}>
                    <div style={{padding:value, background:"honeydew", height:"fit-content", width:"fit-content", display:"flex", justifyContent:"center",border:"0.5px solid #738f73"}}>
                        <div style={{background:"#e3e3e3", height:"25px", width:"25px"}}></div>
                    </div>
                    <p><span>${key}</span> = <span>{value}</span></p>
                </div>

        ))
    }
</div>

<br/>

# Margin

<div style={{display:"flex", flexWrap:"wrap", gap:"30px"}}>
    {
        Object.entries(layoutAndSpacing.margin).map(([key,value])=>(

                <div style={{display:"flex", flexDirection:"column", alignSelf:"end"}}>
                    <div style={{ background:"#F9CC9E", height:"fit-content", width:"fit-content", display:"flex", justifyContent:"center"}}>
                        <div style={{margin:value,background:"#e3e3e3", height:"25px", width:"25px"}}></div>
                    </div>
                    <p><span>${key}</span> = <span>{value}</span></p>
                </div>

        ))
    }
</div>

<br/>

# Border Radius

<div style={{display:"flex", justifyContent:"space-between", margin:"40px 0px"}}>
    {
        Object.entries(layoutAndSpacing.borderRadius).map(([key,value])=>(
             <div style={{display:"flex", flexDirection:"column"}}>
                <div style={{height:"100px",width:"100px",borderRadius:value, border:"1px solid black"}}></div>
                <div style={{marginTop:"10px",fontSize:"14px"}}>{value}</div>
                <div style={{marginTop:"10px",fontSize:"14px"}}>${key}</div>
            </div>
        ))
    }
</div>

<br/>



# Box Shadow

<div style={{display:"flex", justifyContent:"space-between", margin:"40px 0px"}}>
    {
        Object.entries(layoutAndSpacing.boxShadow).map(([key,value])=>(
             <div style={{display:"flex", flexDirection:"column"}}>
                <div style={{height:"100px",width:"100px",boxShadow:value}}></div>
                <div style={{marginTop:"10px",fontSize:"14px"}}>${key}</div>
                <div style={{marginTop:"10px",fontSize:"14px"}}>{value}</div>
            </div>
        ))
    }
</div>