/**
 * Shared cell utilities for **AppDataTable** (`AppDataTable.tsx`).
 *
 * Rows use the `group/table-row` name on `<TableRow>`; use with `onRowClick` for directory-style tables.
 */
export const APP_DATA_TABLE_PRIMARY_ROW_LABEL_CLASS =
  "text-foreground transition-colors group-hover/table-row:text-primary group-data-[state=selected]/table-row:text-primary";
